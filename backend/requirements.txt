# --- Core web framework ---
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
python-multipart>=0.0.9

# --- Data engine (streaming / columnar; avoid loading huge data into pandas) ---
polars>=1.0
duckdb>=1.0
pyarrow>=15.0
pandas>=2.0        # small final tables only
numpy>=1.26

# --- Statistics ---
scipy>=1.11
statsmodels>=0.14

# --- Reporting / plotting ---
matplotlib>=3.8

# --- Optional: real Hugging Face dataset loading (demo mode works without it) ---
datasets>=2.18
huggingface_hub>=0.22

# --- Optional: richer enrichment libraries (self-contained ORA used by default) ---
# gseapy>=1.1
# goatools>=1.3

# --- Optional: Supabase persistence (JSON store used by default) ---
# supabase>=2.4

# --- Testing ---
pytest>=8.0
httpx>=0.27
